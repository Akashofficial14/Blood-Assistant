<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Password | Blood Assistant</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center p-4">

    <main class="w-full max-w-md bg-white rounded-2xl shadow-2xl border border-gray-100 p-8 animate-in fade-in duration-700">
        
        <div class="text-center mb-8">
            <div class="flex justify-center mb-4">
                <div class="bg-red-600 p-3 rounded-2xl shadow-lg shadow-red-200">
                    <i data-lucide="droplet" class="text-white w-8 h-8"></i>
                </div>
            </div>
            <h1 class="text-2xl font-black text-gray-900 tracking-tight">SET NEW PASSWORD</h1>
            <p class="text-gray-500 text-sm mt-2">Secure your account for Blood Assistant</p>
        </div>

        <form action="/api/auth/update-password/<%= user_id %>" method="POST" id="updateForm" class="space-y-6">
            
            <div>
                <label class="text-xs font-bold text-gray-600 uppercase ml-1 tracking-widest">New Password</label>
                <div class="relative mt-2">
                    <span class="absolute inset-y-0 left-0 pl-4 flex items-center text-gray-400">
                        <i data-lucide="lock" class="w-4 h-4"></i>
                    </span>
                    <input 
                        type="password" 
                        name="password" 
                        id="password"
                        required
                        minlength="6"
                        placeholder="••••••••"
                        class="w-full rounded-xl border border-gray-200 p-3.5 pl-11 outline-none transition-all focus:ring-2 focus:ring-red-500 focus:border-transparent"
                    >
                </div>
            </div>

            <div>
                <label class="text-xs font-bold text-gray-600 uppercase ml-1 tracking-widest">Confirm Password</label>
                <div class="relative mt-2">
                    <span class="absolute inset-y-0 left-0 pl-4 flex items-center text-gray-400">
                        <i data-lucide="shield-check" class="w-4 h-4"></i>
                    </span>
                    <input 
                        type="password" 
                        id="confirmPassword"
                        required
                        placeholder="••••••••"
                        class="w-full rounded-xl border border-gray-200 p-3.5 pl-11 outline-none transition-all focus:ring-2 focus:ring-red-500 focus:border-transparent"
                    >
                </div>
                <p id="error-text" class="text-[11px] text-red-500 mt-2 ml-1 font-semibold hidden">Passwords do not match!</p>
            </div>

            <button 
                type="submit" 
                class="w-full bg-red-600 py-4 rounded-xl font-bold text-white shadow-lg shadow-red-100 hover:bg-red-700 transition-all active:scale-[0.98]"
            >
                Update Password
            </button>
        </form>

        <div class="mt-8 text-center text-xs text-gray-400 font-medium">
            USER ID: <span class="font-mono text-gray-300 uppercase tracking-tighter"><%= user_id %></span>
        </div>
    </main>

    <script>
        // Initialize Lucide Icons
        lucide.createIcons();

        // Client-side validation to prevent bad requests
        const form = document.getElementById('updateForm');
        const pass = document.getElementById('password');
        const confirmPass = document.getElementById('confirmPassword');
        const errorText = document.getElementById('error-text');

        form.addEventListener('submit', (e) => {
            if (pass.value !== confirmPass.value) {
                e.preventDefault();
                errorText.classList.remove('hidden');
                confirmPass.classList.add('border-red-500', 'bg-red-50');
            }
        });

        // Hide error when user types again
        confirmPass.addEventListener('input', () => {
            errorText.classList.add('hidden');
            confirmPass.classList.remove('border-red-500', 'bg-red-50');
        });
    </script>
</body>
</html> -->